// Single source of truth for default page sizes across the admin panel.
// Before this existed, every feature invented its own default (20, 20, 25,
// several bare inline numbers) with no shared reasoning for why one screen
// showed more/fewer rows than another.
export const DEFAULT_PAGE_SIZE = 20;
